# Default values for bulk-inbound-service.
debug:
  enable: false
  params: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:9000"

# Number of Pod Replicas desired
replicaCount: 2

# Required Values for the Chart
required:
  # Define the docker image
  image:
    # The repository where is stored the docker image "bulk-inbound-service"
    repository: europe-west3-docker.pkg.dev/gematik-all-infra-prod/demis-dev
    # The name of the image
    name: bulk-inbound-service
    # Overrides the image tag whose default is the chart appVersion. 
    # If not given, the Chart AppVersion will be used
    tag: null

# Extra Labels to be added
# E.g.
# customLabels:
#   my-label: my-label-value
customLabels: {}

# Extra labels for deployments
deploymentLabels: {}

# Define custom Container Entrypoint and Arguments (e.g. JVM Tuning)
# If not given, the Entrypoint defined in Dockerfile will be used
application:
  entrypoint: []
  parameters: []

imagePullPolicy: IfNotPresent
# Define Image Pull Secrets if using private registry
imagePullSecrets: []
nameOverride: "bulk-inbound-service"
fullnameOverride: "bulk-inbound-service"

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

# Annotations for Workload
annotations: {}

# Annotations for Pods
podAnnotations:
  prometheus.io/scrape: "true"
  prometheus.io/path: "/actuator/prometheus"
  prometheus.io/port: "8080"

# Environment Variables to be defined
customEnvVars:
  JAVA_TOOL_OPTIONS: "-XX:ActiveProcessorCount=2 -XX:MaxRAMPercentage=40.0 -XX:MaxMetaspaceSize=192m -XX:+UseG1GC"
  TZ: "Europe/Berlin"
  DB_URL: "jdbc:postgresql://pgbouncer.demis.svc.cluster.local:5432/demis-bulk"
  DB_USER: ars-bulk-user
  LIQUIBASE_DB_USER: ars-bulk-ddl
  PARAM_LIQUIBASE_DB_USER_PURGER: ars-bulk-purger
  SPRING_LIQUIBASE_ENABLED: false
  SPRING_RABBITMQ_USERNAME: svc-bulk-inbound-service
  SPRING_RABBITMQ_VIRTUAL_HOST: bulk

# RabbitMQ credentials secret (injected via envFrom)
# Secret must contain keys: SPRING_RABBITMQ_USERNAME, SPRING_RABBITMQ_PASSWORD, SPRING_RABBITMQ_VIRTUAL_HOST
rabbitmqSecret:
  password:
    name: bis-rabbitmq-credentials
    key: SPRING_RABBITMQ_PASSWORD
    propPath: spring/rabbitmq/password

secret:
  dbPwdUser:
    name: ars-bulk-user-database-secret
    key: ARS_BULK_USER_DB_PASSWORD
    propPath: spring/datasource/password
  dbPwdDdl:
    name: ars-bulk-ddl-database-secret
    key: ARS_BULK_DDL_DB_PASSWORD
    propPath: spring/liquibase/password
  hashPepper:
    name: ars-bulk-upload-hmac-secret
    key: ARS_BULK_UPLOAD_HMAC_SECRET
    propPath: app/hash/pepper
  encryptionCurrent:
    name: ars-bis-in-queue-encryption-secret
    key: ARS_BIS_IN_QUEUE_ENCRYPTION_CURRENT_KEY
    propPath: app/encryption/current-key
  encryptionPrevious:
    name: ars-bis-in-queue-encryption-secret
    key: ARS_BIS_IN_QUEUE_ENCRYPTION_PREVIOUS_KEY
    propPath: app/encryption/previous-key

# Pod Security Context - applies to all containers in a Pod
podSecurityContext: 
  # The GID to tell the Kubelet to modify permissions of a volume owned by a pod
  fsGroup: 10000
  # Determines when to apply fsGroup actions - "Always" is the default
  fsGroupChangePolicy: "OnRootMismatch"
  # Which GID container process is assigned
  runAsGroup: 10000
  # Determine whether container process starts as root (UID 0)
  runAsNonRoot: true
  # Which UID container process is assigned
  runAsUser: 10000

# Container Security Context for a single container - Takes precedence over PodSecurityContext
securityContext:
  #defines whether container can be granted privileges beyond the parent process spawing the process
  allowPrivilegeEscalation: false
  #Define whether the container has privileged access e.g. CAP_SYS_ADMIN capability and SELinux spc_t type
  privileged: false
  #Usually no longer required. Determines whether /proc is masked to the container. Do not mount /proc from a host to a container
  procMount: Default
  # The SELinux context to be applied to the container. Must not be explicitly set to Unconfined.
  seccompProfile: 
    type: "RuntimeDefault"
  #Which GID container process is assigned
  runAsGroup: 10000
  # Determine whether container process starts as root (UID 0)
  runAsNonRoot: true
  #Which UID container process is assigned
  runAsUser: 10000
  # Sets the file system as read-only
  readOnlyRootFilesystem: true
  #Ability to remove and /or add Linux Capabilities
  capabilities:
    drop: 
    - all

service:
  type: ClusterIP
  ports:
    http: 8080

# Default Resources
resources: 
  limits:
    memory: "650Mi"
  requests:
    cpu: "200m"
    memory: "650Mi"

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

istio:
  enable: true
  validateJwt: true
